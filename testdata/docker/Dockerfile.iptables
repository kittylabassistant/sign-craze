FROM golang:1.25-alpine

# Устанавливаем iptables, ipset и iproute2 для интеграционных тестов.
# Проект монтируется через docker run -v — COPY не используется.
RUN apk add --no-cache iptables ip6tables ipset iproute2 bash

WORKDIR /src
