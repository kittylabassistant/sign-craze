# Конфиг nfqws2. Сгенерировано sign-craze. Не редактировать вручную.
# Источник параметров: README nfqws2-keenetic (MIT).
ISP_INTERFACE={{ .ISPInterface }}
NFQWS_QUEUE_NUM={{ .QueueNum }}
POLICY_NAME={{ .PolicyName }}
NFQWS_HOSTLIST="{{ if .HostlistPath }}{{ .HostlistPath }}{{ end }}"
NFQWS_ARGS="{{ .Args }}{{ if .HostlistPath }} --hostlist={{ .HostlistPath }}{{ end }}"
NFQWS_ARGS_QUIC="{{ .ArgsQUIC }}{{ if .HostlistPath }} --hostlist={{ .HostlistPath }}{{ end }}"
NFQWS_ARGS_UDP="{{ .ArgsUDP }}{{ if .HostlistPath }} --hostlist={{ .HostlistPath }}{{ end }}"
